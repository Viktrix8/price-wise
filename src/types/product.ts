export type ScrappedProduct = {
    title: string,
    description: string,
    price: number,
    stockStatus: string,
    isInSale: boolean,
    originalPrice: number | null,
    url: string
    productImageUrl: string,
    rating: number,
    ratingCount: number
}  
